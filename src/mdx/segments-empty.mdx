Try exploring the database and save smaller groups of data into logical segments that can be verified or be used for monitor

Data segments allow us to formulate groupings of data. Those groupings are the result applying a search/filter query over the whole data set. Think of segments like data that belongs to an incident or a court case,  or simply all the videos (type) that contain keywords on their title.
