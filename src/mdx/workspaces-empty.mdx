A workspace contains a single database and directory, or maps to a single remote workspace.
Think of a workspace as a thematic collection of sources, and data gathered from those sources and the investigations that will come up from that data.
